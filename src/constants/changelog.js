export const LATEST_UPDATE = {
  version: '1.0.54',
  description: "Removed the Nearest/Category/A-Z sort bar from the map view, since it didn't do anything there — it's still there in list view.",
}
